export type User = {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    username: string;
    role: "staff" | "manager";
    createdAt: string;
    updatedAt: string;

};

